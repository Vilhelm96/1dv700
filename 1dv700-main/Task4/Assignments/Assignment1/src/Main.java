/**
 * The type Main.
 */
public class Main {
    /**
     * The entry point of application.
     *
     * @param args the input arguments
     */
    //Assume that there is a prepared file with content inside it.
    public static void main(String[] args)
    {
        Menu menu = new Menu();
        menu.mainMenu();
    }
}
